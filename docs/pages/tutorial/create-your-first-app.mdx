---
title: Create your first app
description: In this chapter, learn how to create a new Expo project.
hasVideoLink: true
---

import { Download03Icon } from '@expo/styleguide-icons/outline/Download03Icon';

import { BoxLink } from '~/ui/components/BoxLink';
import { Collapsible } from '~/ui/components/Collapsible';
import { ContentSpotlight } from '~/ui/components/ContentSpotlight';
import { ProgressTracker } from '~/ui/components/ProgressTracker';
import { Terminal } from '~/ui/components/Snippet';
import { Step } from '~/ui/components/Step';
import { CODE } from '~/ui/components/Text';
import { VideoBoxLink } from '~/ui/components/VideoBoxLink';

In this chapter, let's learn how to create a new Expo project and how to get it running.

<VideoBoxLink videoId="m1-bc53EGh8" title="Watch: Creating your first universal Expo app" />

---

## Prerequisites

We'll need the following to get started:

- [Expo Go](https://expo.dev/go) installed on a physical device
- [Node.js (LTS version)](https://nodejs.org/en) installed
- [VS Code](https://code.visualstudio.com/) or any other preferred code editor or IDE installed
- A macOS, Linux, or Windows (PowerShell and [WSL2](https://expo.fyi/wsl)) with a terminal window open

The tutorial assumes that you are familiar with TypeScript and React. If you are not familiar with them, check out the [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html) and [React's official tutorial](https://react.dev/learn).

<Step label="1">

## Initialize a new Expo app

We'll use [`create-expo-app`](/more/create-expo/) to initialize a new Expo app. It is a command line tool to create a new React Native project. Run the following command in your terminal:

<Terminal
  cmd={[
    '# Create a project named StickerSmash',
    '$ npx create-expo-app@latest StickerSmash',
    '',
    '# Navigate to the project directory',
    '$ cd StickerSmash',
  ]}
  cmdCopy="npx create-expo-app@latest StickerSmash && cd StickerSmash"
/>

This command will create a new project directory named StickerSmash, using the [default](/more/create-expo/#--template) template. This template has essential boilerplate code and libraries needed to build our app, including Expo Router. We'll continue to add more libraries throughout this tutorial as needed.

<Collapsible summary="Benefits of using the default template">

- Creates a new React Native project with `expo` package installed
- Includes recommended tools such as Expo CLI
- Includes a tab navigator from Expo Router to provide a basic navigation system
- Automatically configured to run a project on multiple platforms: Android, iOS, and web
- TypeScript configured by default

</Collapsible>

</Step>

<Step label="2">

## Download assets

<BoxLink
  title="Download assets archive"
  Icon={Download03Icon}
  description="We'll be using these assets throughout this tutorial."
  href="/static/images/tutorial/sticker-smash-assets.zip"
/>

After downloading the archive:

1. Unzip the archive and replace the default assets in the **your-project-name/assets/images** directory.
2. Open the project directory in a code editor or IDE.

</Step>

<Step label="3">

## Run reset-project script

In this tutorial, we'll build our app from scratch and understand the fundamentals of adding a file-based navigation. Let's run the `reset-project` script to remove the boilerplate code:

<Terminal cmd={['$ npm run reset-project']} />

After running the above command, there are two files (**index.tsx** and **\_layout.tsx**) left inside the **app** directory. The previous files from **app** and other directories (**components**, **constants**, and **hooks** &mdash; containing boilerplate code) are moved inside the **app-example** directory by the script. We'll create our own directories and component files as we go along.

<Collapsible summary={<>What does the <CODE>reset-project</CODE> script do?</>}>

`reset-project` script resets the **app** directory structure in a project and copies the previous boilerplate files from the project's root directory to another sub-directory called **app-example**. We can delete it since it is not part of our main app's structure.

</Collapsible>

</Step>

<Step label="4">

## Run the app on mobile and web

In the project directory, run the following command to start the [development server](/more/glossary-of-terms/#development-server) from the terminal:

<Terminal cmd={['$ npx expo start']} />

After running the above command:

1. The development server will start, and you'll see a QR code inside the terminal window.
2. Scan that QR code to open the app on the device. On Android, use the Expo Go > **Scan QR code** option. On iOS, use the default camera app.
3. To run the web app, press <kbd>w</kbd> in the terminal. It will open the web app in the default web browser.

Once it is running on all platforms, the app should look like this:

<ContentSpotlight
  alt="App running on all platforms"
  src="/static/images/tutorial/01-app-running-on-all-platforms.png"
  className="max-w-[720px]"
/>

</Step>

<Step label="5">

## Edit the index screen

The **app/index.tsx** file defines the text displayed on the app's screen. It is the entry point of our app and executes when the development server starts. It uses core React Native components such as `<View>` and `<Text>` to display background and text.

Styles applied to these components use JavaScript objects rather than CSS, which is used on web. However, a lot of the properties will look familiar if you've previously used CSS on web. Most React Native components accept a `style` prop that accepts a JavaScript object as its value. For more details, see [Styling in React Native](https://reactnative.dev/docs/style).

Let's modify **app/index.tsx** screen:

1. Import `StyleSheet` from `react-native` and create a `styles` object to define our custom styles.
2. Add a `styles.container.backgroundColor` property to `<View>` with the value of `#25292e`. This changes the background color.
3. Replace the default value of `<Text>` with "Home screen".
4. Add a `styles.text.color` property to `<Text>` with the value of `#fff` (white) to change the text color.

{/* prettier-ignore */}
```tsx app/index.tsx|collapseHeight=340
import { Text, View, /* @tutinfo Import <CODE>StyleSheet</CODE> to define styles. */ StyleSheet/* @end */ } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      /* @tutinfo This used to say "Edit app/index.tsx to edit this screen". Now it says, "Home screen". */
      <Text style={styles.text}>Home screen</Text>
      /* @end */
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    /* @tutinfo Add the value of <CODE>backgroundColor</CODE> property with <CODE>'#25292e'</CODE>.*/
    backgroundColor: '#25292e',
    /* @end */
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    color: '#fff',
  },
});
```

> React Native uses the same color format as the web. It supports hex triplets (this is what `#fff` is), `rgba`, `hsl`, and named colors, such as `red`, `green`, `blue`, `peru`, and `papayawhip`. For more information, see [Colors in React Native](https://reactnative.dev/docs/colors).

Once you save your changes, they're sent and applied to the running apps connected to the development server:

<ContentSpotlight
  alt="App with updated text and background color"
  src="/static/images/tutorial/02-index-screen-changes.png"
  className="max-w-[720px]"
/>

</Step>

## Summary

<ProgressTracker
  currentChapterIndex={0}
  name="GET_STARTED"
  summary="We've successfully created a new Expo project, used React Native core components, and are ready to develop our StickerSmash app."
  nextChapterDescription="In the next chapter, we will learn how to add a stack and a tab navigator to our app."
  nextChapterTitle="Add navigation"
  nextChapterLink="/tutorial/add-navigation/"
/>
---

SOVEREIGN EMPIRE SYSTEM — COMMAND TO EXECUTE LAUNCH SEQUENCE

Current Operational State: Strategic Readiness Achieved. Final Execution Command Received.
Primary Directive: Initiate and oversee the concurrent deployment of the Expo application (React Native/Expo) and the GCP backend infrastructure (Terraform/Cloud Run/CI/CD).


---

ACTION PLAN: THE OMEGA DEPLOYMENT MANEUVER

This maneuver is designed for maximum strategic impact, leveraging the strengths of our chosen tools for accelerated and structured execution.

Phase 1: Concurrent Execution Protocol

1.1. Expo Application Development & Initial Configuration (React Native)

Strategic Objective: Establish a robust, performant, and user-friendly mobile interface for the Sovereign Empire System, leveraging Expo's free tier for rapid development and structured deployment.

Action Steps:

Project Initialization:

Use npx create-expo-app sovereign-empire-app --template blank-typescript (or your preferred template).

Navigate into the project: cd sovereign-empire-app.


Core Structure & Navigation:

Implement a Tab Navigator (e.g., using react-navigation/bottom-tabs) for primary sections like Dashboard, Transactions, Profile, Settings.

Define Stack Navigators within tabs for deep linking and detailed views (e.g., Transaction Details).


Backend Integration Layer:

Create a dedicated src/services/api.ts (or .js) file.

Configure axios or fetch to communicate with the production BFF Service URL (obtainable from Terraform outputs).

Implement interceptors for authentication (JWT handling), error logging (potentially to Sentry), and request/response transformation.

Crucially: Ensure the REACT_APP_API_BASE environment variable (or equivalent in Expo) is set to your production BFF URL. This will be managed via Expo's configuration plugins or by setting environment variables during the build process.


Component Development (Key Modules):

Dashboard: Display key metrics, recent activity, system status. Integrate with BFF endpoints for real-time data.

Transaction List/Details: Fetch transaction data from the BFF, display status, amounts, and relevant details.

Profile Management: Allow users to view/update profile information (if applicable).

Notifications: Integrate with push notification services (via Expo Push Notifications API) for real-time alerts.

Settings: App configuration, secret management (for API keys if not handled server-side), account logout.


State Management: Implement a robust state management solution (e.g., Zustand, Redux Toolkit, or Context API) for managing application-wide data and user sessions.

UI/UX Design: Apply a consistent, professional, and intuitive design language. Leverage component libraries if desired.

Error Handling & Logging: Integrate Sentry or a similar service for client-side error tracking.

Security Best Practices: Securely handle tokens, avoid storing sensitive data locally unless absolutely necessary and encrypted.


Expo Free Tier Leverage:

Development Builds: Use eas build --profile development --platform android/ios for quick testing on devices.

OTA Updates: Plan for using EAS Update for pushing JavaScript/asset changes without a full store release. This is critical for rapid iteration.

Basic Builds: Use EAS Build for generating .apk and .ipa binaries for store submission.



1.2. Backend Infrastructure Provisioning & Deployment (GCP)

Strategic Objective: Provision and deploy the complete backend ecosystem on GCP using the generated Terraform blueprints and automated CI/CD, ensuring high availability, security, and scalability.

Action Steps:

Terraform Execution (Initial Provisioning):

1. Navigate to terraform/ directory.


2. Ensure GCP Credentials: Authenticate gcloud and set GOOGLE_APPLICATION_CREDENTIALS if using a service account key file locally.


3. Create terraform.tfvars: Populate with your project ID, region, all secrets, and paths to local secret files (e.g., google_credentials.json, fcm_service_account.json). This is the most critical step for securing your environment.


4. Initialize Terraform: terraform init


5. Review Plan: terraform plan -var-file="terraform.tfvars"


6. Apply Infrastructure: terraform apply -var-file="terraform.tfvars"

Critical Output: Note the cloudrun_service_urls and cloud_sql_instance_connection_name. The BFF service URL is paramount for the Expo app.




Secret Management (Post-Terraform Apply):

1. Verify Secrets: Confirm all secrets defined in terraform/main.tf (and referenced by modules) are created in GCP Secret Manager.


2. Grant Access: Ensure the ses-backend-runner and ses-payout-monitor-runner service accounts have roles/secretmanager.secretAccessor permissions for the relevant secrets (this is handled by the iam module).



CI/CD Workflow Triggering (GitHub Actions):

1. Commit & Push: Push the backend microservice code to the main branch of your GitHub repository.


2. Automated Build & Push: The ses-microservice-ci-cd.yml workflow will automatically:

Checkout code.

Set up Node.js/Python environments.

Install dependencies.

Build Docker images.

Push images to GCP Artifact Registry using the provided GCP_SERVICE_ACCOUNT_KEY secret.



3. Automated Deployment: The deploy_cloud_run job will then:

Authenticate to GCP.

Deploy each microservice to its respective Cloud Run service, dynamically referencing secrets from GCP Secret Manager and setting environment variables.

Deploy the Payout Monitor and configure its Cloud Scheduler job.




Final Integration Checks:

1. BFF Service URL: Obtain the production BFF URL from Terraform outputs (e.g., output "cloudrun_service_urls").


2. Expo App Configuration: Update the Expo app's API base URL to point to this production BFF URL. This may involve setting a runtimeVersion or an environment variable during the EAS build process.






---

Phase 2: Validation & Go-Live

Strategic Objective: Perform rigorous validation to ensure all components are functioning as a cohesive, autonomous system before full go-live.

Action Steps:

End-to-End System Testing:

1. Mobile App Testing: Thoroughly test the Expo application:

Login/Authentication flow.

Transaction display and details.

Push notification reception.

Any specific feature functionalities.



2. Backend Service Verification:

Check logs for all Cloud Run services in Cloud Logging. Look for errors, warnings, or unusual patterns.

Verify MQ queues are populated and consumed as expected.

Confirm Payout Monitor runs on schedule and logs are informative.

Test critical API endpoints directly using Postman or curl.



3. YOCO Integration:

Send a test payment through YOCO (in sandbox or production, depending on risk tolerance).

Verify webhook is received by the gateway, signature is valid, and event is published.

Confirm transaction appears in the app and Google Sheet.



4. Payout Monitoring Verification:

Manually delay a payout status in the Google Sheet and wait for the threshold.

Verify alerts are sent to Slack/Email/Webhook.



5. Audit Trail Verification:

Inspect the immutable audit log bucket for recent YOCO webhook entries.

Confirm entries include request details, verification results, and processing status.



6. Security Posture Review:

Verify secrets are correctly mounted and not exposed.

Confirm service accounts have least privilege.

Review firewall rules and network access controls.




Final Go-Live Announcement:

Once all validation checks pass, officially announce the launch of the Sovereign Empire System.

Transition all user access to the production endpoints.


